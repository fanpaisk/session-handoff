---
name: session-handoff
description: Use when a session's deliverable has passed acceptance and the conversation is about to be handed off or abandoned — or when a long session feels bloated and expensive. Writes the session's durable knowledge into AGENTS.md, docs/STATUS.md, and docs/DECISIONS.md so a fresh session with an empty conversation can reconstruct the project, re-runs verification at handoff time instead of trusting memory, audits for dangling session references, competing status documents and leaked credentials, and then checks the handoff actually works. Also use when the user asks to "交接", "收尾", "开新会话", "清理上下文", or "hand off". Not for merely shrinking a session you intend to keep using — use compaction for that.
---

# 会话交接（session-handoff）

把当前会话里"关于项目的知识"搬进持久文件，让会话可以被廉价丢弃。留在会话里的只剩工作台状态（正在追的调试链路）。

**知识沉淀在文件里，不在会话里培养。**

## 何时用

- 一个功能**验收通过**后，准备开新会话继续下一个功能。
- 会话已经变长变贵，你打算弃用它。
- 把工作交接给另一个人或另一个 agent。

## 何时不用

- **只想给当前会话瘦身、但仍继续用同一个会话** → 用压缩（compaction / `/compact`）。交接的产出是给新会话的，不是给自己瘦身的。这两件事经常被混为一谈。
- 本次没有改动任何文件、也没有产生可复用知识（例如一次性的问答）。
- **功能尚未验收通过** → 先验收。若用户明确要求此时交接，照做，但必须在 `STATUS.md` 顶部标注"未验收"。

## 关键机制（决定了交接必须长什么样）

- `AGENTS.md` **自动加载**：从项目根到 cwd 的整条指令链作为持久基线，进入每个新会话的第一次请求。（ZCode 的指令文件也是 `AGENTS.md`；Claude Code 是 `CLAUDE.md`。）
- `docs/STATUS.md` 与 `docs/DECISIONS.md` **不会自动加载**。
- 所以闭环必须由 `AGENTS.md` 里的一条**强制指针**完成。缺了它，新会话"什么也不说"是看不懂项目的——这是最常见的交接失败原因。

## 工作流程

### 1. 定位项目根，盘版本控制与既有文档

取最近的存在 `.git` 的祖先目录；没有则以当前工作目录为根。**若项目根不在 git 下，必须向用户报告**：这三份文件是项目知识的唯一载体，却没有版本保护。这一条优先于代码备份——**代码可以重写，理由和踩过的坑不能**。

然后扫描**既有的同类文档**（`进度*.md`、`TODO.md`、`ROADMAP.md`、`PLAN.md`、`HANDOFF.md`、旧的 `NOTES.md`）：过时且无独立信息 → 删除；仍有价值 → 吸收进三份文件；有独立价值（如长篇设计稿）→ 保留，但**顶部加一行指针**指向 `docs/STATUS.md` 并注明其角色。

**不许留下两份都在讲"下一步"却不说明谁是权威。**

### 2. 盘点本次会话

**验证必须当场重跑，不许凭记忆写。** 实际执行 `git status`（若在 git 仓库内）与项目声明的构建、测试、验收命令，用**刚刚拿到的真实输出**作为证据。一个已经压缩过的长会话，对"测试通过"的记忆可能对应一个**已经不存在的检出状态**——记忆不是证据，重跑才是。

然后回答五个问题；答不上来的部分就是必须显式标注的缺口，不要用流畅措辞掩盖：

1. 做了什么，**验收命令与当下的真实输出**
2. 学到了什么下次必须知道的（新增约束、踩过的坑、被证伪的假设）
3. 做了什么决定，**为什么**，否掉了什么方案
4. 状态：哪些已验收、哪些半成品、哪些没开始
5. 下一个会话的第一件事

### 3. 写进三个文件

每条知识只有一个家。同一个事实出现在两个文件里，就是未来两份互相矛盾的真相。

| 文件 | 装什么 | 自动加载 |
|---|---|---|
| `AGENTS.md` | 宪法：开工前必读指针、定位、硬约束、构建/测试/验收命令、已知的坑 | 是 |
| `docs/STATUS.md` | 状态：交付物与验收情况、下一步、未决问题、已知缺陷 | 否 |
| `docs/DECISIONS.md` | 理由：每条决定 + 为什么 + 被否方案与原因 | 否 |

写作规则：

- **`AGENTS.md` 的预算按整个文件计量，不按标记块——自动加载的是整个文件。** 标记块 ≤ 60 行；**整个文件 ≤ 一屏（约 60 行）**，后者才是真实成本。超出时：**块内**内容超了 → 理由搬 `DECISIONS.md`、手册搬独立 skill；**项目原有的非交接内容**顶超了 → 那部分不是交接职责，**不擅自删改**，向用户报告并建议拆到子目录 `AGENTS.md`（按目录就近加载）或独立 skill。两种情况都**不删约束本身**。
- **`AGENTS.md` 用幂等标记块，不靠判断力去合并。** 交接职责的全部内容包在 `<!-- SESSION_HANDOFF:START -->` 与 `<!-- SESSION_HANDOFF:END -->` 之间；`SESSION_HANDOFF:START` 全文件**只许出现一次**。
  - **标记已存在** → 只替换标记之间的内容，标记外一个字都不动。
  - **文件存在但无标记** → 先分辨原有内容。里面若已有**交接产物**（上面那五类内容，标题措辞可能不同，如「一句话定位」「常用命令」），那是**上一轮交接的输出、不是用户内容**：**并入标记块并去重**。若是用户自己写的规则，才把标记块**追加**到末尾，不重排、不改写。
  - **分不清某一节属于哪类 → 问用户，不要猜。**
  - 直接追加已存在的交接内容会造出**两套互相矛盾的指令**——比占篇幅危险得多。
- **不写会话流水账。** 调试过程、时间线、"先试了 A 又试了 B"属于对话，不属于文件。
- **不写指向会话的悬空引用。** 「见会话」「见上文」「如前所述」会让知识随会话**静默失效**——文件读起来完全正常，内容却已经没了。证据只在会话里时：把关键内容搬进文件，或标 `UNKNOWN`（见下）并给出重验入口。
- **不确定的知识只有两种合法形态。** 有办法验证 → 给出**验证入口**（可重跑的命令）。没法验证 → `UNKNOWN：<缺什么> —— <需要向谁确认>`，一个**可检索的具体标记**，不是一种态度。**禁止用模糊词掩盖未知**（"应该能跑"、"大概没问题"）。**在编造和留白之间，永远选留白——留空的可修复，编造的不可修复。**
- **`STATUS.md` 覆盖式更新，`DECISIONS.md` 追加式更新。** 状态是当下快照，理由是历史；推翻一条决定时新增一条并注明取代了哪条，不改写旧条目。
- **验收标准必须可执行**：能写出命令、检查步骤或预期输出，否则"已验收"不成立。**"代码就绪但未实测"是独立的一档。** 在 `DECISIONS.md` 里立下的验收标准（如"一致率 ≥99.9%"），**必须在 `AGENTS.md` 命令节给出可执行入口**，否则没人能重跑它。
- **"下一步"必须指明落点与前提**：改哪个文件、怎么复现环境、改完要不要重启。只写"修缺陷 X"而不给落点，接手者仍然开不了工。
- **破坏性验证命令必须写明恢复步骤**（例如临时改配置跑完要还原），否则中途中断会留下错误状态。

模板与填好的样例见 [references/templates.md](references/templates.md)。

### 4. 交接自检

逐条确认，任何一条不通过就回去补：

- [ ] `AGENTS.md` 含强制开工指针，且指针指向的文件真实存在
- [ ] 每个"已验收"都附了**本次交接当场重跑**的命令与输出
- [ ] "下一步"含落点（文件名）与环境复现方式
- [ ] `DECISIONS.md` 每条决定都有理由；涉及技术权衡的有被否方案（纯用户指令型写"用户指定／用户拒绝"即可）
- [ ] `DECISIONS.md` 里的每条验收标准，都能在 `AGENTS.md` 命令节找到可执行入口
- [ ] 无「见会话／见上文／如前所述」这类**指向会话的悬空引用**
- [ ] 不确定处要么有验证入口，要么标了 `UNKNOWN`，**没有被模糊词掩盖**
- [ ] `SESSION_HANDOFF:START` 只出现一次（`grep -c` 必须返回 `1`），且标记外内容一字未动
- [ ] 没有同一条知识被写进两个文件；标记块内无重复的交接内容
- [ ] 项目里**没有其他文档也在讲"下一步"**（或已加指针说明谁是权威）
- [ ] 标记块 ≤ 60 行，**整个 `AGENTS.md` ≤ 一屏**；超了已向用户报告总量与拆分建议
- [ ] **没有任何凭据、密钥、连接串或隐私信息**
- [ ] **交接文件本身受版本控制**；若否，已向用户报告
- [ ] 交接报告已输出，且列出了剩余的 `UNKNOWN` 条目

### 5. 输出交接报告

写完文件不算交接结束。必须输出五节——某节为空写"无"，**不要省略标题**。

````markdown
## 交接报告

**1. 变更文件**
- `AGENTS.md` —— <新建标记块 / 更新了块内哪些部分；块外未动>
- `docs/STATUS.md` —— <覆盖式更新了什么>
- `docs/DECISIONS.md` —— <新增了哪几条编号>
- <其他：退役的竞争文档、加了指针的文件>

**2. 当前状态**
<一句话：能不能用、能用到什么程度>

**3. 下一个会话如何起步**
<可直接粘贴的开场白>

**4. 剩余 UNKNOWN**
- `UNKNOWN：<缺什么> —— <需要向谁确认>`
- 或：无

**5. 风险与待确认**
- <无版本控制 / 发现的密钥 / 竞争文档的处理结果 / 需要用户拍板的事>
- 或：无
````

第 3 节的开场白形如：

```
读 AGENTS.md、docs/STATUS.md、docs/DECISIONS.md，然后复述：这个项目是什么、
有哪些硬约束、当前状态、下一步做什么。复述完再动手。
```

## 安全约束

交接文件会被**自动加载**，而且很可能被提交进 git。绝不写入 API key、token、密码、私钥、连接串、内网地址、带凭据的 URL 或隐私信息。

需要引用凭据时，只写**在哪里获取**（环境变量名、密码管理器条目名），不写值。若发现项目文件里**已经存在**密钥：从交接文档中删除、向用户报告、并建议**轮换该凭据**——已经进过 git 的密钥，删除文件是没用的。

## 失败处理

| 情况 | 处理 |
|---|---|
| 定位不到项目根（无 `.git`） | 以当前工作目录为根，**并说明这一点** |
| 拿不准某一节内容属于用户规则还是上一轮交接产物 | **问用户**，不擅自删改、不擅自去重 |
| 发现已存在第二个 `SESSION_HANDOFF` 标记块 | 合并回一个块；`SESSION_HANDOFF:START` 只能出现一次 |

## 交接是否真的成功：空会话测试

这是唯一可靠的验收方式：开一个**全新会话**（空对话），输入上面的开场白（指针写到位的话，只说"继续"也应该够），要求它先复述"项目是什么、硬约束、当前状态、下一步"。

复述正确 → 交接成功。复述含糊或错误 → **文件有缺口，补文件**。

**绝不要用"回到老会话解释一遍"来修补。** 那正是本 skill 要消除的依赖；一旦这么做，你就又回到了每轮为陈旧上下文付费的状态。

## 不要做的事

- 不要把整段对话摘要粘进文件——那是把压缩的毛病抄进文件系统。
- 不要把老会话留作"知识备份"。文件是唯一真相；老会话与文件冲突时，以文件为准。
- 不要在没有测试或可执行验收标准的项目上宣称交接完成——那种项目的脆弱性不是文档能修的，先补验收手段。
